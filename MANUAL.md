# 📖 [기능 매뉴얼] UBUS DragonRPA & 통합 자산 라벨 자동화 시스템 (MANUAL.md)

본 문서는 UBUS DragonRPA 시스템의 실무 조작 및 운영을 위한 공식 사용자 매뉴얼입니다.

---

## 🚀 1. 빠른 시작 가이드 (Quick Start)

### ① 에이전트 실행
1. `print-agent/dist/` 폴더에서 **`[에이전트실행.bat]`**을 더블클릭합니다.
2. 검은 콘솔 창과 함께 브라우저 모니터링 창(`http://127.0.0.1:9988`)이 열리면 준비 완료입니다.

### ② 웹 대시보드 접속
- 웹 브라우저로 시스템 메인 페이지(`http://localhost:5173/LabelPrintStation/`)에 접속합니다.

---

## 🖨️ 2. 라벨 인쇄 & 블루투스 스캐너 운용법

### ① 무선 블루투스 스캐너 1초 무인 출력
- 화면의 어느 곳을 클릭해 두었든 상관없이(Zero-Focus), 블루투스 스캐너로 자산번호나 시리얼 바코드를 `삐빅` 찍습니다.
- 시스템이 0.1초 만에 자산을 식별하고, 활성화된 라벨 서식대로 Zebra 프린터에서 라벨이 즉시 1초 만에 출력됩니다.

### ② 서식 프리셋 변경
- 상단 **`[라벨 서식 디자인]`** 탭으로 이동합니다.
- 상단 드롭다운에서 `자산 대형 72×40mm`, `소형 QR 50×25mm`, `제조번호 QR` 중 원하는 규격을 선택하면 즉시 적용됩니다.

---

## 🤖 3. RPA 무인 자동화 3단계 실행법

1. **1단계: 시나리오 선택**
   - **`[RPA 실행]`** 탭에서 수행할 작업(`입고 자동 등록`, `자산 정보 수정`, `출고 검수`)을 선택합니다.
2. **2단계: 데이터 지정**
   - **`[📥 표준 양식 다운로드]`**를 눌러 엑셀 템플릿을 내려받아 데이터를 기입합니다.
   - 작성된 엑셀 파일을 업로드 영역에 끌어다 놓습니다. (컬럼 순서 무관 자동 인식)
3. **3단계: 실행**
   - **`[▶ RPA 실행]`** 버튼을 클릭하면 PC 에이전트가 브라우저를 띄워 무인 자동화를 완수합니다.

---

## 🔐 4. 관리자 모드 진입 및 시나리오 편집법

1. **관리자 인증**:
   - `[RPA 시나리오 편집]` 또는 `[스키마 빌더]` 탭을 클릭하면 인증 창이 뜹니다.
   - 초기 비밀번호 **`0000`**을 입력하고 `[인증]`을 누릅니다.
2. **노코드 시나리오 편집**:
   - 좌측에서 `[텍스트 입력]`, `[버튼 클릭]` 등의 블록을 추가합니다.
   - `[🔍 화면에서 요소 집기]`를 눌러 웹 화면에서 원하는 버튼을 클릭하면 XPath가 0.1초 만에 자동 기입됩니다.
   - `{{자산번호}}`, `{{선반번호}}` 등의 변수를 클릭하여 입력값에 삽입하고 `[시나리오 저장]`을 누릅니다.
3. **관리자 세션 잠금**:
   - 작업 완료 후 상단 우측의 **`[🔓 관리자 모드 (클릭 시 잠금)]`**을 누르면 즉시 잠금 상태로 전환됩니다.

---

## 🔄 5. 에이전트 원클릭 재시작 (Kill + Restart)

- 로컬 에이전트 대시보드(`http://127.0.0.1:9988`) 우측 상단의 **`[🔄 에이전트 재시작]`** 버튼을 클릭하면, 에이전트가 스스로 프로세스를 재기동하여 설정을 즉시 갱신합니다.
